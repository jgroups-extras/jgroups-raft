
<!--
  Default stack using IP multicasting. It is similar to the "udp"
  stack in stacks.xml, but doesn't use streaming state transfer and flushing
  author: Bela Ban
-->

<config xmlns="urn:org:jgroups"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">
   <TCP 
         bind_addr="${jgroups.bind_addr:loopback}"
         bind_port="${jgroups.bind_port:7800}"
         thread_pool.min_threads="0"
         thread_pool.max_threads="200"
         thread_pool.keep_alive_time="30000"

         bundler_type="org.jgroups.protocols.TransferQueueBundler"
         max_bundle_size="64K" />

    <MPING mcast_addr="228.1.2.3" />
    <MERGE3 max_interval="30000"
            min_interval="10000"/>
    <FD_SOCK/>
    <!--FD_ALL3/ -->
    <!-- VERIFY_SUSPECT timeout="1500"  /-->
    <pbcast.NAKACK2 use_mcast_xmit="false"/>
    <UNICAST3 ack_threshold="50000"
              conn_close_timeout="60000"/>
    <pbcast.STABLE desired_avg_gossip="50000"
                   max_bytes="4M"/>
    <raft.NO_DUPES/>
    <pbcast.GMS print_local_addr="true" join_timeout="1000"/>
    <!--BATCH flush_interval="1"
                max_batch_size="50"  /-->

    <!--BATCH2 flush_interval="50"    
           poll_timeout="100"
           max_batch_size="50" /-->

    <UFC max_credits="2M"
         min_threshold="0.4"/>
    <MFC max_credits="2M"
         min_threshold="0.4"/>
    <FRAG2 frag_size="60K"  />
    <raft.ELECTION election_min_interval="100"
                   election_max_interval="500"/>
    <raft.RAFT members="${raft_members:A,B,C}"
               raft_id="${raft_id}"
               log_dir="${log_dir}" />
    <raft.REDIRECT/>
    <raft.CLIENT bind_addr="0.0.0.0" />
</config>
